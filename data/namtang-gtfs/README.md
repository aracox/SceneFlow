# Namtang GTFS feed

Raw GTFS text files copied locally from:

`/Users/itsaretboonsong/Downloads/ACC/data/namtang-gtfs/`

The raw `.txt` files are intentionally gitignored because the feed is large
and `shapes.txt` is over GitHub's normal 100 MB file limit. Keep this folder
for local processing, and commit smaller derived files when the app needs them.
